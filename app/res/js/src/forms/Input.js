import React, { Component } from 'react';

/**
 * Abstract Input class. Used for instanceof/type checking.
 */
export default class Input extends Component {
    constructor(props) {
        super(props);
    }
}
